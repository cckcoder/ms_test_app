pipeline {
  agent any

  stages {
    stage('Verify Branch') {
      steps {
        sh 'printenv'
      }
    }
    stage('Docker Build') {
      steps {
        sh(script: 'docker --version')
        //sh(script: """
          //docker build -t cckcoder/fastapi:latest .
          //docker images -a
        //""")
      }
    }
  }
}
